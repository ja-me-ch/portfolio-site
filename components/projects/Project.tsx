import React from "react";
import { styled } from "@mui/material";
import Preview from "../../interfaces/preview.interface";

interface IProps {
    preview: Preview
}

const Project = function ({ preview }: { preview: Preview }) {
    console.log(preview);
    
    
    return <div></div>;
};

export default Project;
