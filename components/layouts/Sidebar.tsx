import React from 'react';
import { styled } from '@mui/material';

const RootStyle = styled("div")(() => ({
    backgroundColor: "#D3C9B9",
}));

function Sidebar() {
  return (
    <div>
      SideBar
    </div>
  )
}

export default Sidebar
